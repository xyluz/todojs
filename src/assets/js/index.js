let todoItems = [];

// create a function to add new todos
function addTodo(item) {
  const todo = {
    text,
    checked: False
  };
  todoItems.push(todo);

  const list = document.querySelector("");
}
